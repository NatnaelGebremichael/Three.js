name: Auto Merge

on:
  pull_request:
    branches-ignore:
      - main

jobs:
  autoMerge:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v3

      - name: Auto Merge
        run: echo "event name is:" ${{ github.event_action }}
        env:
          GH_TOKEN: ${{ secrets.Act_Local_GitWorkFlowTesting }}
